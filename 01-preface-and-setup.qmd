# Preface and Setup

:::cdi-message
- **ID:** DBSQL-F-L01
- **Type:** Preface
- **Audience:** Public
- **Theme:** Building the data backbone
:::

Relational databases are formal systems for structuring data @codd1970relational.

This track focuses on structural clarity before analysis.

---

## Connecting to SQLite

```{python}
#| label: connect-sqlite
#| echo: true

import sqlite3
import pandas as pd

con = sqlite3.connect("data/cdi-retail.sqlite")
print(con)
```

The output shows a `sqlite3.Connection` object.  
This is not a server connection. SQLite is file-based and embedded.

---

## Inspecting Tables

```{python}
#| label: list-tables
#| echo: true

tables = pd.read_sql(
    "SELECT name FROM sqlite_master WHERE type='table';",
    con
)
print(tables)
```

This confirms available tables.

---

Structure → Query → Interpretation

